= render partial: "top_nav"

#content
  %ul#facts.list-group
    - @facts.each do |fact|
      %li.list-group-item
        %h2.fact= fact.description
        %aside.sources
          %i.fa.fa-bar-chart
          = source_links_for fact

  = paginate @facts
